import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.constanza.godelivery',
  appName: 'Delivery',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
